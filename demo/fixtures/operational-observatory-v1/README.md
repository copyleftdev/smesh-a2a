# Operational observatory v1 fixture

This fixture is generated by `operational-observatory-project` from the verified two-event `full-matrix-replay-v1` cross-language vector.

- `actors.json`: local actor/site labels for the recorded `alpha` and `beta` producers.
- `editorial.json`: event-ID-keyed local narration/cues.
- `source-facts.json`: empty closed source-facts input for this two-event contract vector.
- `browser-bootstrap.json`: canonical expected replay-input digest, run seal, and final presentation duration commitment consumed by the browser boundary.
- `package.jsonl`: deterministic `operational-observatory/1` projection.
- `receipt.json`: canonical projection receipt.

It is suitable as the Wave 2 browser contract fixture. It does not claim a six-organization run, production geography, or any fact absent from the replay. The legacy `demo/lifeline.trace.jsonl` remains synthetic and separate.
